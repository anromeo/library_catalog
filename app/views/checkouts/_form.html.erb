<%= form_for(@checkout) do |f| %>
  <% if @checkout.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@checkout.errors.count, "error") %> prohibited this checkout from being saved:</h2>

      <ul>
      <% @checkout.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <b>Book:</b>
    <%= f.collection_select :book_id, Book.all, :id, :title  %>
  </div>
  <div class="field">
    <b>Patron:</b>
    <%= f.collection_select :patron_id, Patron.all, :id, :firstname %>
  </div>
  <div class="field">
    Due Date
    <%= f.date_select :duedate %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
